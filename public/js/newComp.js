document.addEventListener("DOMContentLoaded", () => {
    
    function dropdown() {
        $(".ui.dropdown").dropdown();
    };

    dropdown();

    // headers.forEach(h2 => {
    //     h2.addEventListener('click', () => {
    //         window.location.href = h2.dataset.href;
    //     });
    // });

});